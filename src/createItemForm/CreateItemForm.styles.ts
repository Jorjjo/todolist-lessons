import { SxProps } from '@mui/material';

export const createItemContainerSx: SxProps = {
    display: 'flex',
    alignItems: 'flex-start', //чтобы при оишбке иконка '+' не уезжала 
    gap: '12px',
};
